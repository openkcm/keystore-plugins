version: 3

includes:
  shared:
    taskfile: hack/common/Taskfile_plugin.yaml
    flatten: true
    vars:
      CODE_DIRS: '{{.ROOT_DIR}}/cmd/... {{.ROOT_DIR}}/internal/...'
      COMPONENTS: 'keystoreop/aws'
      GENERATE_DOCS_INDEX: "false"
      REPO_URL: 'https://github.com/openkcm/keystore-plugins'
