name: Quality

on:
  push:
    tags:
      - v*
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - 'LICENSES/**'
      - '.releases/**'
      - '.github/**'
  pull_request:

permissions:
  contents: read
  pull-requests: write


jobs:
  check:
    uses: openkcm/build/.github/workflows/ci.lib.yaml@main
    secrets: inherit
