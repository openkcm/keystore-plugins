name: Labeler

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  do:
    uses: openkcm/build/.github/workflows/labeler.yaml@main
    secrets: inherit
